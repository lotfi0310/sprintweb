<!DOCTYPE html>

<title>ReserEvenement</title>

{% block body %}
    <h1>ReserEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdReser</th>
                <td>{{ reser_evenement.idReser }}</td>
            </tr>
            <tr>
                <th>DateReservation</th>
                <td>{{ reser_evenement.dateReservation ? reser_evenement.dateReservation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reser_evenement_index') }}">back to list</a>

    <a href="{{ path('app_reser_evenement_edit', {'idReser': reser_evenement.idReser}) }}">edit</a>

    {{ include('reser_evenement/_delete_form.html.twig') }}
{% endblock %}
