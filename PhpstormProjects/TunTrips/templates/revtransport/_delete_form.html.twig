<form method="post" action="{{ path('app_revtransport_delete', {'id': revtransport.id}) }}" onsubmit="
return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ revtransport.id) }}">
    <button class="btn">Delete</button>
</form>
